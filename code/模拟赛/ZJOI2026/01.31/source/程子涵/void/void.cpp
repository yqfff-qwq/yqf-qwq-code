#include<bits/stdc++.h>
//#define int long long
using namespace std;
signed main() {
	freopen("void.in", "r", stdin);
	freopen("void.out", "w", stdout);
	ios::sync_with_stdio(false);
	cin.tie(0);
	cout.tie(0);
	int T;
	cin >> T;
	while (T--) {
		int n, m;
		cin >> n >> m;
		for (int i = 1; i <= m; i++) {
			int x, y;
			cin >> x >> y;
		}
		cout << "1 2" << endl;
	}
	return 0;
}

#include<bits/stdc++.h>
//#define int long long
using namespace std;
signed main() {
	freopen("tower.in", "r", stdin);
	freopen("tower.out", "w", stdout);
	ios::sync_with_stdio(false);
	cin.tie(0);
	cout.tie(0);
	int T;
	cin >> T;
	while (T--) {
		int n, m;
		cin >> n >> m;
		for (int i = 1; i <= n * 2; i++) {
			int x;
			cin >> x;
		}
		cout<<m-1<<endl;
	}
	return 0;
}
